# Image Classifier

Basic image classification logic implemented as a simple script.

Currently uses a placeholder approach to test how input data can be processed and categorized.

## Usage

```bash
python classifier.py
```

## Current state

This is more of a starting point. The model can be replaced with a proper ML-based approach later.